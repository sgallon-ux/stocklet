{
  "name": "stocklet-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Pruebas de las reglas de seguridad de Firestore y Storage de Stocklet.",
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-stocklet \"node runner.cjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0"
  }
}
